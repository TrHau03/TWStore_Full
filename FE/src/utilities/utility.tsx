import { Dimensions } from "react-native";
import { COLORS } from ".";

export const { width: WIDTH, height: HEIGHT } = Dimensions.get('window');
export const PADDING_HORIZONTAL = 16;
export const PADDING_TOP = 20;
export const BG_COLOR = COLORS.white;